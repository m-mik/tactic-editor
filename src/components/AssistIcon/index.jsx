import React from 'react';
import { green500 } from 'material-ui/styles/colors';
import BallIcon from '../BallIcon';

const AssistIcon = props => (
  <BallIcon color={green500} {...props} />
);

export default AssistIcon;
