import React from 'react';
import { red900 } from 'material-ui/styles/colors';
import BallIcon from '../BallIcon';

const OwnGoalIcon = props => (
  <BallIcon color={red900} {...props} />
);

export default OwnGoalIcon;
