import React from 'react';
import { black } from 'material-ui/styles/colors';
import BallIcon from '../BallIcon';

const GoalIcon = props => (
  <BallIcon color={black} {...props} />
);

export default GoalIcon;
