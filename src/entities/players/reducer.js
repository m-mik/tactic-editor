// TODO: update reducer
const player = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

const byId = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default byId;
